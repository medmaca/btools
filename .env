# Can place a copy of this in the home directory to override package defaults

#####################################################
#   Display Configuration for btools pre view       #
#####################################################

# Unified display settings (single adaptive wrapped mode)
VIEW_COLS_PER_SECTION=10       # How many columns to show per section when wrapping
VIEW_MAX_COL_WIDTH=15         # Maximum width for any column
VIEW_ELLIPSIS=...              # Text to show when truncating

# TOML output settings
VIEW_OUT_UNIQUE_MAX=50         # Maximum unique values to show in TOML output

# Performance settings
VIEW_LAZY_LOAD=True           # Use lazy loading for very large datasets (slower for small files)


#####################################################
#        Settings for  pre_select_data.py           #
#####################################################
# GZIP output settings
GZIP_OUT=False                 # Write output files as gzip compressed (.gz extension)
